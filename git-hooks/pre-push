#!/bin/sh
#
# pre-push hook
#
# Delegates to bd pre-push logic (if available).
#

# --- bd (beads) pre-push delegation ---
if command -v bd >/dev/null 2>&1; then
    bd hooks run pre-push "$@"
    BD_EXIT=$?
    if [ "$BD_EXIT" -ne 0 ]; then
        exit "$BD_EXIT"
    fi
fi

exit 0
